const numbers = [100, 200, 300, 400, 500, 600];
const primeiroNumber = numbers[0];
const [primary, segunda, , , quinto] = numbers; //pega valores vazios
const [primero, segundo, ...resto] = numbers; //...(nome varivel) significa pegar o resto do  array


console.log(primeiroNumber);